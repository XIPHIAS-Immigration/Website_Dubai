# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/

# Environment files — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-debug.log*
yarn-error.log*
dev-*.log
dev-*.err.log
dev-*.out.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/settings.json
*.swp
*.swo

# Claude / AI tooling
.claude/
.codex/
.xiphias-platform/

# Large media — videos are too large to commit / push (kept local only)
public/videos/
public/images/personal/video/
*.mp4
*.webm
*.mov
*.m4v
*.mkv

# Misc
.vercel
*.tsbuildinfo
next-env.d.ts

# Runtime data store — contains real customer/lead PII, must never be committed
your/persistent/platform-store.json

# Build rollback/backup folders left by deploy-production.ps1 (large, disposable)
.next.*

# Runtime data store — ALL variants, including timestamped backups.
# These contain real customer names, emails and phone numbers.
your/
platform-store*.json

# Legacy env templates that still hold live credentials.
# Tracked historically — see SECURITY note; do not add new ones.
env.production
env.download
